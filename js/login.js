const users=window.SEED.employees;const demo=document.getElementById('demoUsers');users.slice(0,6).forEach(u=>{let b=document.createElement('button');b.textContent=u.title;b.onclick=()=>{username.value=u.email;password.value=u.password};demo.appendChild(b)});loginForm.onsubmit=e=>{e.preventDefault();let u=users.find(x=>x.email===username.value.trim()&&x.password===password.value.trim());if(!u)return alert('بيانات الدخول غير صحيحة');localStorage.setItem('aleman_user',JSON.stringify(u));location.href='dashboard.html'};